You want to use a package called figlet

if you type it in it woll say

> figlet: command not found

This means that the packages is not installed on the system

On a distribution with [[deb-package]]s you can use 

> apt-cache search packagname

